<!DOCTYPE html>
<html lang="en" data-baseurl="{{ '/' | url }}">
<head>
  <meta charset="UTF-8" />
  <title>MATHesis{% if title %} – {{ title }}{% endif %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}" />
  <link rel="icon" href="{{ '/assets/favicon.ico' | url }}" type="image/x-icon" />
  <link rel="preload" as="image" href="{{ '/assets/logo.png' | url }}" />
  <link rel="preload" as="image" href="{{ '/assets/logotype.svg' | url }}" />


</head>
<body{% if page.url == "/" %} class="home-floating-nav"{% endif %}>

<!-- Logo Header + Navigation -->
<header class="site-header">
  <div class="logo-container">
    <img src="{{ '/assets/logo.png' | url }}" class="logo-icon" width="50" height="50" loading="eager" decoding="async" />
    <img src="{{ '/assets/logotype.svg' | url }}" class="letter-logo" width="140" height="32" loading="eager" decoding="async" />
  </div>

  <nav class="site-nav">
    <a href="{{ '/' | url }}">Home</a>
    <a href="{{ '/search/' | url }}">Search</a>
    <a href="{{ '/transcriptions/' | url }}">Transcriptions</a>
    <a href="{{ '/visualization/' | url }}">Visualization</a>
    <a href="{{ '/map/' | url }}">Map</a>
    <a href="{{ '/about/' | url }}">About</a>
  </nav>
</header>


  <main>
    {{ content }}
  </main>
</body>
</html>
